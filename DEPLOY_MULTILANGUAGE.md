# 🚀 Деплой системы мультиязычности на Render

## ⚠️ Важно!

Локальный бот и Render используют **разные базы данных**. Чтобы мультиязычность заработала на Render, нужно:
1. Загрузить обновленные файлы на GitHub
2. Выполнить миграцию БД на Render
3. Перезапустить сервисы

---

## 📁 Файлы для загрузки

### Обязательные файлы:
- ✅ `bot_full_verification.py` - бот с мультиязычностью
- ✅ `translations.py` - система переводов (НОВЫЙ ФАЙЛ)
- ✅ `migrate_add_language.py` - миграция БД (НОВЫЙ ФАЙЛ)

### Опциональные (если изменились):
- `mini_app.py` - веб-сервер
- `app_new.py` - альтернативный бот
- `templates/` - шаблоны мини-приложения

---

## 🔄 Пошаговая инструкция

### Шаг 1: Загрузка на GitHub

1. **Откройте папку GITHUB_DEPLOY**
2. **Скопируйте файлы в ваш GitHub репозиторий:**
   - `bot_full_verification.py`
   - `translations.py` (новый)
   - `migrate_add_language.py` (новый)

3. **Сделайте commit и push:**
   ```bash
   git add bot_full_verification.py translations.py migrate_add_language.py
   git commit -m "Add multilanguage support (RU/EN/UK)"
   git push origin main
   ```

### Шаг 2: Миграция БД на Render

**ВАЖНО:** Выполните миграцию ПЕРЕД перезапуском бота!

#### Вариант A: Через Render Shell (рекомендуется)

1. Откройте ваш сервис на Render
2. Перейдите в **Shell** (вкладка слева)
3. Выполните команду:
   ```bash
   python migrate_add_language.py
   ```
4. Дождитесь сообщения: "✅ МИГРАЦИЯ ЗАВЕРШЕНА УСПЕШНО"

#### Вариант B: Автоматически при деплое

Добавьте в `render.yaml` или в настройки сервиса:
```yaml
buildCommand: "pip install -r requirements.txt && python migrate_add_language.py"
```

### Шаг 3: Перезапуск сервисов

1. **Перезапустите бота:**
   - Render → Ваш бот-сервис → Manual Deploy → Deploy latest commit

2. **Перезапустите веб-сервер (если нужно):**
   - Render → Ваш web-сервис → Manual Deploy → Deploy latest commit

### Шаг 4: Проверка

1. Откройте бота: https://t.me/noscamnftrbot
2. Отправьте `/start`
3. Проверьте выбор языка

---

## 🔍 Проверка миграции

### Через Render Shell:

```bash
# Проверить структуру таблицы
python -c "import sqlite3; conn = sqlite3.connect('data/unified.db'); cursor = conn.cursor(); cursor.execute('PRAGMA table_info(users)'); print([col[1] for col in cursor.fetchall()])"

# Проверить наличие колонки language
python -c "import sqlite3; conn = sqlite3.connect('data/unified.db'); cursor = conn.cursor(); cursor.execute('PRAGMA table_info(users)'); cols = [col[1] for col in cursor.fetchall()]; print('✅ language найдена' if 'language' in cols else '❌ language НЕ найдена')"
```

---

## 📊 Что изменится после деплоя

### В боте:
- ✅ При первом `/start` пользователь видит выбор языка
- ✅ Все тексты на выбранном языке
- ✅ Кнопка "🌍 Изменить язык" в меню
- ✅ Язык сохраняется в БД

### В мини-приложении:
- ⏳ Пока без изменений (требуется дополнительная интеграция)
- ⏳ Будущая задача: передавать язык из бота в мини-приложение

---

## 🐛 Возможные проблемы

### Проблема 1: Бот не запускается после деплоя

**Причина:** Отсутствует файл `translations.py`

**Решение:**
```bash
# Проверьте наличие файла
ls -la translations.py

# Если отсутствует - загрузите на GitHub
```

### Проблема 2: Ошибка "no such column: language"

**Причина:** Миграция не выполнена

**Решение:**
```bash
# Выполните миграцию вручную
python migrate_add_language.py
```

### Проблема 3: Язык не сохраняется

**Причина:** База данных не обновлена

**Решение:**
```bash
# Проверьте структуру БД
python -c "import sqlite3; conn = sqlite3.connect('data/unified.db'); cursor = conn.cursor(); cursor.execute('PRAGMA table_info(users)'); print([col[1] for col in cursor.fetchall()])"

# Если language отсутствует - выполните миграцию
python migrate_add_language.py
```

---

## 📝 Требования

### Python пакеты (должны быть в requirements.txt):
```
aiogram>=2.25.1
telethon>=1.28.5
flask>=2.3.0
requests>=2.31.0
```

### Структура БД после миграции:
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uid TEXT,
    telegram_id TEXT UNIQUE NOT NULL,
    username TEXT,
    first_name TEXT,
    phone TEXT,
    balance_stars INTEGER DEFAULT 0,
    balance_rub REAL DEFAULT 0,
    balance_uah REAL DEFAULT 0,
    successful_deals INTEGER DEFAULT 0,
    verified BOOLEAN DEFAULT FALSE,
    session_file TEXT,
    language TEXT DEFAULT 'ru',  -- НОВАЯ КОЛОНКА
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## ✅ Чек-лист деплоя

- [ ] Файлы скопированы в GITHUB_DEPLOY
- [ ] Файлы загружены на GitHub
- [ ] Commit и push выполнены
- [ ] Миграция БД выполнена на Render
- [ ] Бот перезапущен на Render
- [ ] Веб-сервер перезапущен (если нужно)
- [ ] Протестирован выбор языка в боте
- [ ] Проверена смена языка
- [ ] Проверены все 3 языка (RU/EN/UK)

---

## 🎯 После деплоя

### Тестирование:
1. Откройте бота: https://t.me/noscamnftrbot
2. Отправьте `/start`
3. Выберите язык
4. Проверьте все кнопки
5. Проверьте смену языка

### Мониторинг:
- Проверьте логи на Render
- Убедитесь, что нет ошибок
- Проверьте, что язык сохраняется в БД

---

## 🔮 Будущие улучшения

### Интеграция с мини-приложением:

1. **Передача языка в URL:**
   ```python
   web_app_url = f"https://nft-gifts-market-uid.onrender.com?lang={user_lang}"
   ```

2. **API для получения языка:**
   ```python
   @app.route('/api/user_language')
   def get_user_language():
       user_id = request.args.get('user_id')
       # Вернуть язык из БД
   ```

3. **Локализация мини-приложения:**
   - Создать файлы переводов для JS
   - Добавить переключение языка в интерфейсе
   - Синхронизировать с языком бота

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на Render
2. Убедитесь, что миграция выполнена
3. Проверьте наличие всех файлов
4. Перезапустите сервисы

---

**Дата:** 2026-02-09
**Версия:** 1.0
**Статус:** Готово к деплою
