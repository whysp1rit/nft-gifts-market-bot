# ⚡ Быстрый деплой мультиязычности

## 🚀 3 простых шага

### 1️⃣ Загрузите файлы на GitHub

```bash
# Из папки GITHUB_DEPLOY скопируйте в ваш репозиторий:
- bot_full_verification.py
- translations.py (НОВЫЙ)
- migrate_add_language.py (НОВЫЙ)

# Commit и push
git add .
git commit -m "Add multilanguage support"
git push
```

### 2️⃣ Выполните миграцию на Render

**Откройте Render Shell и выполните:**
```bash
python migrate_add_language.py
```

**Ожидаемый результат:**
```
✅ Колонка 'language' успешно добавлена!
✅ МИГРАЦИЯ ЗАВЕРШЕНА УСПЕШНО
```

### 3️⃣ Перезапустите бота

**Render → Ваш бот → Manual Deploy → Deploy latest commit**

---

## ✅ Готово!

Откройте бота и проверьте:
- `/start` → Выбор языка
- Все тексты на выбранном языке
- Кнопка "Изменить язык" работает

---

## ⚠️ Если что-то не работает

### Бот не запускается:
```bash
# Проверьте наличие translations.py
ls -la translations.py
```

### Ошибка "no such column: language":
```bash
# Выполните миграцию
python migrate_add_language.py
```

### Язык не сохраняется:
```bash
# Проверьте БД
python -c "import sqlite3; conn = sqlite3.connect('data/unified.db'); cursor = conn.cursor(); cursor.execute('PRAGMA table_info(users)'); print([col[1] for col in cursor.fetchall()])"
```

---

**Подробная инструкция:** DEPLOY_MULTILANGUAGE.md
